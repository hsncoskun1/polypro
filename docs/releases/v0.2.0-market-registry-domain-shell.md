# Release Note — v0.2.0 Market Registry Domain Shell

## Purpose
Establish the market registry domain layer as the backbone for future discovery, API, and lifecycle management features.

## What Was Done
- `backend/app/domain/markets/model.py` — `Market` dataclass, `Timeframe` enum (1D/1W/1M/3M), `MarketStatus` enum (active/inactive)
- `backend/app/domain/markets/exceptions.py` — `DuplicateMarketError`, `MarketNotFoundError`, `InvalidTimeframeError`
- `backend/app/domain/markets/registry.py` — `InMemoryMarketRegistry` (add/get/list/update-status) + `parse_timeframe()` helper
- `backend/tests/domain/test_market_registry.py` — 10 domain tests

## Test Summary
- Framework: pytest
- Tests: 10 / 10 PASSED
- Coverage: empty list, add, duplicate reject, get, missing reject, active filter, status update, invalid timeframe

## Commit
See branch `feat/v0.2.0-market-registry-domain-shell`

## Branch / Merge
- Branch: `feat/v0.2.0-market-registry-domain-shell`
- Status: pushed to remote, not yet merged

## Risks / Notes
- No API routes — domain only
- In-memory storage — no persistence yet
- Registry is not a singleton; caller is responsible for lifecycle
