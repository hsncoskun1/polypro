# Release Note — v0.1.1 Backend Shell

## Purpose
Establish the FastAPI backend foundation as the first independently testable layer of POLYPRO.

## What Was Done
- Created `backend/` directory with `app/` package structure
- `app/main.py` — FastAPI app with lifespan handler (not deprecated `on_event`)
- `app/api/health.py` — `GET /health` endpoint returning `{"status": "ok", "version": "0.1.1"}`
- `app/core/config.py` — env-based config (`APP_ENV`, `APP_HOST`, `APP_PORT`)
- `app/core/logger.py` — `get_logger()` wrapper over stdlib logging
- `backend/requirements.txt` — pinned dependencies
- `backend/tests/test_health.py` — 3 unit tests

## Test Summary
- Framework: pytest + FastAPI TestClient
- Tests: 3 / 3 PASSED
- Coverage: health endpoint status code, version field, status field

## Commit
`a6d4edc` — feat: v0.1.1 backend shell — FastAPI foundation

## Branch / Merge
- Branch: `feat/v0.1.1-backend-shell`
- Merged into main via PR #2 (bundled foundation merge)

## Risks / Notes
- None
