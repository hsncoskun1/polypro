# Release Note — v0.1.2 Frontend Shell

## Purpose
Establish the React + Vite frontend shell with routing, layout, and health indicator.

## What Was Done
- Created `frontend/` with Vite + React + TypeScript + Tailwind v4 (`@tailwindcss/vite`)
- `src/app/Router.tsx` — BrowserRouter with routes: `/`, `/user`, `/admin`
- `src/components/Layout.tsx` — nav bar with NavLink active states + HealthBadge + Outlet
- `src/components/HealthBadge.tsx` — polls `/health`, renders green/red/pulse dot
- `src/routes/Home.tsx`, `UserPanel.tsx`, `AdminPanel.tsx` — real screens, dark theme
- `vite.config.ts` — vitest config with jsdom test environment
- `src/test/routes.test.tsx` — 3 render tests

## Test Summary
- Framework: Vitest + @testing-library/react
- Tests: 3 / 3 PASSED
- Coverage: route rendering for /, /user, /admin

## Commit
`fff6104` — feat: v0.1.2 frontend shell — React + Vite + TS + Tailwind

## Branch / Merge
- Branch: `feat/v0.1.2-frontend-shell`
- Merged into main via PR #2 (bundled foundation merge)

## Risks / Notes
- None
