# v0.2.3 — Market Registry Persistence Shell

## Summary

Markets are now persisted to a JSON file. The in-memory registry is loaded from disk on startup and written on every mutation (create, status update). No data is lost on restart.

## Changes

| File | Change |
|------|--------|
| `backend/app/persistence/__init__.py` | New package |
| `backend/app/persistence/markets.py` | `JsonMarketStore` with `load()` / `save()` |
| `backend/app/main.py` | Store instantiated in lifespan; registry hydrated from file on startup |
| `backend/app/api/markets.py` | `store.save()` called after `POST /markets` and `PATCH /{id}/status` |
| `backend/tests/persistence/__init__.py` | New package |
| `backend/tests/persistence/test_market_store.py` | 7 unit tests for `JsonMarketStore` |
| `backend/tests/api/test_markets_api.py` | `client` fixture updated to use `tmp_path` + `MARKET_STORE_PATH`; 1 persistence integration test added |

## Configuration

| Env var | Default | Description |
|---------|---------|-------------|
| `MARKET_STORE_PATH` | `data/markets.json` | Path to JSON persistence file |

## Test Results

- **40 tests, 40 passed, 0 failed**
  - `tests/api/` — 16 tests (15 existing + 1 new integration)
  - `tests/domain/` — 14 tests
  - `tests/persistence/` — 7 new tests
  - `tests/` — 3 health tests
