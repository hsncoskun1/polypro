# Iteration — v0.3.14 Discovery Trigger Operational Hardening

## Scope

Hardens the fetch-based discovery trigger endpoint from v0.3.13. Explicit timeout bounds, URL fallback/override verification, and expanded test coverage. No new features.

## What Changed

- `backend/app/api/schemas/discovery.py`: `timeout` field now has upper bound `le=60.0`
- `backend/tests/api/test_discovery_api.py`: 5 new tests covering zero timeout, max timeout, config URL fallback, body URL override, timeout wiring

## Versions in Scope

| Version | Role |
|---------|------|
| v0.3.13 | Trigger endpoint wired to fetch-to-discovery chain |
| v0.3.14 | Operational hardening: validation bounds, URL/timeout wiring verified |
| v0.3.15+ | Auth, scheduler, or next feature layer |

## Test Results

155 passed, 1 skipped (Windows), 0 failed. Zero regressions.
