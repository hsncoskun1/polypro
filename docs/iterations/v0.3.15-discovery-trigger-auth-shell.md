# Iteration — v0.3.15 Discovery Trigger Auth Shell

## Scope

Adds static Bearer token auth to `POST /api/v1/discovery/trigger`. Token from env var. No JWT, OAuth, or full user system.

## What Changed

- `backend/app/api/deps.py`: new `verify_trigger_auth` FastAPI dependency
- `backend/app/api/discovery.py`: `Depends(verify_trigger_auth)` added to trigger endpoint
- `backend/app/core/config.py`: `TRIGGER_AUTH_TOKEN` env config added
- `backend/tests/api/test_discovery_api.py`: 4 new auth tests via `auth_client` fixture

## Versions in Scope

| Version | Role |
|---------|------|
| v0.3.14 | Trigger endpoint operational hardening |
| v0.3.15 | Static Bearer token auth shell |
| v0.3.16+ | Auth hardening, role-based access, or scheduler |

## Test Results

159 passed, 1 skipped (Windows), 0 failed. Zero regressions.
