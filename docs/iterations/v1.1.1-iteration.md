# Iteration — v1.1.1 User Rule/Edit Visibility Enforcement Completion Pack

## Scope
Enforces visible_rules + editable_rules entitlement fields on the user panel.

## Decisions
- RulesPanel: known rules are the 6 strategy rules from the domain (time, price, move, spread, event_limit, max_positions)
- Empty visible_rules → all rules visible (consistent with visible_panels semantics)
- editable_rules → marks individual rules as user-configurable vs read-only
- Gate: 'rules' panel key added to UserPanel visible_panels enforcement
- No backend API change needed — entitlement already carries these fields

## Files Changed
- `frontend/src/components/RulesPanel.tsx` (new)
- `frontend/src/routes/UserPanel.tsx`
- `frontend/src/test/rule-visibility.test.tsx` (new)
