# Iteration — v0.3.12 Polymarket Fetch-to-Discovery Shell

## Scope

Adds `run_polymarket_fetch_to_discovery()` — a function that accepts a `PolymarketClient` instance and runs the full fetch-to-discovery chain. `run_polymarket_client_discovery()` refactored to delegate to it.

## What Changed

- `backend/app/services/discovery_client.py`: new `run_polymarket_fetch_to_discovery(client, registry, ...)` function; `run_polymarket_client_discovery()` delegates to it.
- `backend/tests/services/test_discovery_client.py`: new `TestRunPolymarketFetchToDiscovery` suite with injected mock client (9 tests).

## Versions in Scope

| Version | Role |
|---------|------|
| v0.3.9 | Client-discovery integration shell (URL-based) |
| v0.3.11 | Timeframe mapping wired into chain |
| v0.3.12 | Client injection function; URL wrapper delegates |
| v0.3.13+ | Trigger endpoint wiring or scheduler integration |

## Test Results

152 passed, 1 skipped (Windows), 0 failed. Zero regressions.
