# Iteration Summary — v0.2.2 Market Registry Contract Hardening

## Scope
Harden the v0.2.1 market registry API and domain layer before discovery integration.

## Status
Branch pushed, not yet merged to main.

## What Changed
- `backend/app/domain/markets/registry.py` — deterministic list ordering by market_id
- `backend/tests/domain/test_market_registry.py` — 4 new hardening tests
- `backend/tests/api/test_markets_api.py` — 5 new hardening tests

## Architecture Decisions
- Sort by `market_id` in `registry.list()` — cheapest determinism, no secondary index needed
- Pydantic enum validation on `StatusUpdate.status` already enforces valid values → 422 covered without extra handler code

## Tests
- 15 API / 15 PASSED
- 14 domain / 14 PASSED
- 3 health / 3 PASSED
- Total: 32 / 32 PASSED

## Notes
- No new endpoints
- No persistence
- No frontend wired
- Ready for discovery integration after merge
