# Iteration — v0.3.18 Discovery Status Auth Shell

## Scope

Adds Bearer token auth to `GET /api/v1/discovery/status`. Reuses `verify_trigger_auth`. No new auth logic, no new token, no scope expansion.

## What Changed

- `backend/app/api/discovery.py`: `Depends(verify_trigger_auth)` added to GET /status endpoint
- `backend/tests/api/test_discovery_api.py`: 4 new auth tests for status endpoint; all existing GET /status calls updated with `_AUTH_HEADER`

## Auth Behaviour

| Condition | Result |
|-----------|--------|
| `TRIGGER_AUTH_TOKEN` not set | HTTP 500 (fail-closed) |
| Token set, header missing | HTTP 401 |
| Token set, wrong token | HTTP 401 |
| Token set, correct Bearer | HTTP 200 |

## Versions in Scope

| Version | Role |
|---------|------|
| v0.3.17 | Discovery run status shell |
| v0.3.18 | Discovery status auth shell |
| v0.3.19+ | Scheduler or further hardening |

## Test Results

173 passed, 1 skipped (Windows), 0 failed. Zero regressions.
